# --- Núcleo ---
Django==6.0.6
djangorestframework==3.17.1
djangorestframework_simplejwt==5.5.1
django-cors-headers==4.9.0
python-dotenv==1.2.2

# --- Hashing de contraseñas (Argon2id) ---
argon2-cffi==25.1.0
argon2-cffi-bindings==25.1.0

# --- Integración WhaleTV ---
# Selenium controla el portal web para BLOQUEAR televisores (la Device Lock API
# solo desbloquea). Necesita Chrome + chromedriver: ver Dockerfile.
selenium==4.45.0

# --- Reportes e importación en Excel (.xlsx) ---
openpyxl==3.1.5

# --- Despliegue en producción ---
gunicorn==26.0.0
whitenoise==6.12.0
# PostgreSQL en producción; SQLite sigue funcionando en local.
psycopg[binary]==3.3.4
dj-database-url==3.1.2

# --- Transitivas (fijadas para builds reproducibles) ---
asgiref==3.11.1
cffi==2.0.0
pycparser==3.0
PyJWT==2.13.0
sqlparse==0.5.5
tzdata==2026.2
