# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr

# Overrides personales (.env.local, .env.production.local, …).
# `.env` y `.env.production` SÍ se versionan: las variables VITE_ se incrustan
# en el bundle y son públicas, no contienen secretos.
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
