# Desarrollo local — Vite carga este archivo con `npm run dev`.
#
# Vacío a propósito: las peticiones salen a la ruta relativa `/api/...` y el
# proxy de Vite las reenvía a http://127.0.0.1:8000 (ver vite.config.ts).
# Así no hay CORS en local. Levanta Django en el 8000 y listo.
VITE_API_URL=
